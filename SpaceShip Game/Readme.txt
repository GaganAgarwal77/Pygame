A game in which the user controls a spaceship and tries to avoid incoming asteroids which get difficult to avoid with time.

Source for PNG files: Google Images

For the Code to work you need to install the pygame package ,you can do so by doing the following steps:


1.To install pip First update your packege list - sudo apt update

2.Install pip for python2 - sudo apt install python-pip

3.Install pip for python3 - sudo apt install python3-pip

4.Install pygame - sudo pip3 install pygame -sudo pip install pygame
